# Copy this to .env.local for local development
# Production values are in .env.production (committed)
VITE_API_URL=http://localhost:5001

# ⚠️  NEVER add MONGODB_URI or any database credentials here.
#     Backend-only env vars belong in server/.env
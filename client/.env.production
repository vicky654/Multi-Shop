# ── Frontend API URL (PRODUCTION) ─────────────────────────────────
# Vite uses this file automatically when building for production.
# Committed to git — safe because this is a public backend URL.
# Override in the Vercel dashboard if you ever change the backend URL.
VITE_API_URL=https://multishop-backend-9jbg.onrender.com

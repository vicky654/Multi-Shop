node_modules
.env
.next
dist
build
.env.local

# Cypress — never commit real credentials or test artifacts
cypress.env.json
cypress/screenshots
cypress/videos
cypress/results
# Files saved by download assertions in the E2E suite — test output, not source.
cypress/downloads